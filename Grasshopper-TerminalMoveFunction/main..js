function move (position, roll) {
    if(roll === 1){
      return position + (roll * 2)
    }else if(roll === 2){
      return position + (roll * 2)
    }else if(roll === 3){
      return position + (roll * 2)
    }else if(roll === 4){
      return position + (roll * 2)
    }else if(roll === 5){
      return position + (roll * 2)
    }else if(roll === 6){
      return position + (roll * 2)
    }
  }